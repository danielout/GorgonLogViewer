# CDN Reference — Remaining Work

The app auto-introspects CDN JSON files via the Schema view (toggle Tree/Schema when viewing a .json file). This analyzes the JSON structure and displays field types, presence frequency, and sample values automatically.

Individual CDN file documentation could be added here if detailed field-by-field docs are needed beyond what the auto-introspection provides.
